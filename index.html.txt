<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>

  <!-- WhatsApp Preview -->
  <meta property="og:title" content="Someone has a Valentine question for you 💌">
  <meta property="og:description" content="Edith, don’t say no… if you can 😏💕">
  <meta property="og:image" content="https://i.imgur.com/4M7IWwP.png">
  <meta property="og:type" content="website">

  <title>Edith 💕 Will You Be My Valentine?</title>

  <style>
    body {
      margin: 0;
      font-family: 'Arial', sans-serif;
      background: linear-gradient(135deg, #ff9a9e, #fad0c4);
      height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      overflow: hidden;
    }

    .card {
      background: white;
      padding: 25px;
      border-radius: 20px;
      text-align: center;
      width: 90%;
      max-width: 350px;
      box-shadow: 0 10px 30px rgba(0,0,0,0.2);
    }

    h1 {
      color: #ff4d6d;
      margin-bottom: 10px;
    }

    p {
      font-size: 18px;
      margin-bottom: 20px;
    }

    img {
      width: 150px;
      margin-bottom: 15px;
    }

    .buttons {
      position: relative;
      height: 120px;
    }

    button {
      padding: 12px 20px;
      border: none;
      border-radius: 30px;
      font-size: 16px;
      cursor: pointer;
      position: absolute;
      transition: all 0.3s ease;
    }

    #yesBtn {
      background: #ff4d6d;
      color: white;
      left: 50%;
      transform: translateX(-50%);
      box-shadow: 0 0 15px rgba(255,77,109,0.6);
      animation: pulse 1.5s infinite;
    }

    #noBtn {
      background: #ddd;
      color: #333;
      left: 20%;
      top: 60px;
    }

    @keyframes pulse {
      0% { transform: translateX(-50%) scale(1); }
      50% { transform: translateX(-50%) scale(1.1); }
      100% { transform: translateX(-50%) scale(1); }
    }

    .celebrate {
      position: fixed;
      inset: 0;
      background: linear-gradient(135deg, #ff758c, #ff7eb3);
      display: none;
      align-items: center;
      justify-content: center;
      flex-direction: column;
      color: white;
      text-align: center;
      z-index: 10;
    }

    .celebrate h1 {
      font-size: 36px;
    }

    .share {
      margin-top: 20px;
      background: white;
      color: #ff4d6d;
      padding: 12px 25px;
      border-radius: 30px;
      font-size: 16px;
    }
  </style>
</head>

<body>

<audio id="bgMusic" loop>
  <source src="love.mp3" type="audio/mpeg">
</audio>


  <div class="card">
    <h1>Edith 💕💫</h1>
    <img src="https://i.imgur.com/4M7IWwP.png" alt="Love">
    <p>Will you be my Valentine? 💖</p>

    <div class="buttons">
      <button id="yesBtn">Yes 💕</button>
      <button id="noBtn">No</button>
    </div>

    <p style="margin-top:15px;">— From Sebastine 💖</p>
  </div>

  <div class="celebrate" id="celebrate">
    <h1>YAYYYY 💘</h1>
    <p>Edith, you just made me the happiest person alive 🥰</p>
    <p>Happy Valentine’s Day 💞</p>

    <button class="share" onclick="shareWhatsApp()">Share the love 💌</button>
  </div>

  <script>
const music = document.getElementById('bgMusic');

document.body.addEventListener('click', () => {
  if (music.paused) {
    music.volume = 0.5;
    music.play();
  }
}, { once: true });

    const noBtn = document.getElementById('noBtn');
    const yesBtn = document.getElementById('yesBtn');
    const celebrate = document.getElementById('celebrate');

    const noTexts = [
      "No",
      "Are you sure? 😏",
      "Don’t break my heart 💔",
      "Think again 🥺",
      "Edith please 😭",
      "Okay last chance 😌"
    ];

    let count = 0;

    noBtn.addEventListener('mouseover', moveNo);
    noBtn.addEventListener('click', moveNo);

    function moveNo() {
      const x = Math.random() * (window.innerWidth - 120);
      const y = Math.random() * (window.innerHeight - 120);

      noBtn.style.left = x + 'px';
      noBtn.style.top = y + 'px';

      noBtn.innerText = noTexts[Math.min(count, noTexts.length - 1)];
      count++;

      // Make YES more attractive
      yesBtn.style.transform = `translateX(-50%) scale(${1 + count * 0.1})`;
    }

    yesBtn.addEventListener('click', () => {
      celebrate.style.display = 'flex';
      launchHearts();
    });

    function launchHearts() {
      for (let i = 0; i < 30; i++) {
        const heart = document.createElement('div');
        heart.innerText = '💖';
        heart.style.position = 'fixed';
        heart.style.left = Math.random() * window.innerWidth + 'px';
        heart.style.top = '-20px';
        heart.style.fontSize = '24px';
        heart.style.animation = 'fall 3s linear';
        document.body.appendChild(heart);

        setTimeout(() => heart.remove(), 3000);
      }
    }

    function shareWhatsApp() {
      const text = encodeURIComponent(
        "Someone has a Valentine question for you 😏💖\n\nClick here 👇"
      );
      const url = encodeURIComponent(window.location.href);
      window.open(`https://wa.me/?text=${text}%20${url}`, '_blank');
    }
  </script>

  <style>
    @keyframes fall {
      to {
        transform: translateY(110vh);
        opacity: 0;
      }
    }
  </style>

</body>
</html>
